Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

The controller and CRDs have been installed in namespace {{ .Release.Namespace }}.
{{- if and .Values.webhook.enabled .Values.certManager.enabled }}
Admission webhooks are enabled and use cert-manager-managed certificates.
{{- end }}
{{- if .Values.metrics.serviceMonitor.enabled }}
If the cluster has the ServiceMonitor CRD, a ServiceMonitor may be rendered for HTTPS metrics scraping.
{{- end }}
{{- if and .Values.metrics.enabled .Values.certManager.enabled .Values.metrics.certManager.enabled }}
cert-manager-backed metrics TLS is enabled and the manager will mount a chart-managed metrics serving certificate.
{{- end }}

Verify the installation:

  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get customresourcedefinitions hermesagents.hermes.nous.ai
  helm status {{ .Release.Name }} -n {{ .Release.Namespace }}

Next steps:

  1. Review chart values and production deployment knobs:
       helm get values {{ .Release.Name }} -n {{ .Release.Namespace }}
       docs/helm-values.md
  2. Create a HermesAgent from the sample catalog under config/samples/
  3. Describe the resource for status and events:
       kubectl describe hermesagent <name> -n <namespace>
  4. Read the user docs:
       README.md
       docs/api-reference.md
       docs/helm-values.md
       docs/troubleshooting.md
