# Minimal sample set applied by `kubectl apply -k config/samples/`.
# Additional scenario-specific samples are documented in README.md and should
# be applied directly with `kubectl apply -f`.
resources:
- hermes_v1_hermesagent.yaml
# +kubebuilder:scaffold:manifestskustomizesamples
